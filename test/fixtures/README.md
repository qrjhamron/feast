# Test Fixtures

This directory stores packet captures, world snapshots, and other binary test
fixtures used by unit and integration tests.

Currently empty – fixtures are created here as needed when hardening tests are added.
